import { StyleSheet } from "react-native/types";

const styles = StyleSheet.create({
    container: {
         flex: 1, 
         position: "relative" 
    }

    scrollContainer: {
        
    }
})

export default styles